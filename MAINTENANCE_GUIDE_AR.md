## 📧 نظام الإرسال الاحترافي (Nodemailer Relay)

لقد قمنا بترقية الموقع لنظام إرسال **مستقر للأبد** ولا يحتاج لتوثيق المستلمين. سيعمل الموقع مع سامر وحيدر وآية فوراً. لكي يعمل، اتبع هذه الخطوات البسيطة:

1.  **تجهيز حساب Gmail:** استخدم حساب Gmail (يفضل حساب جديد للموقع).
2.  **تفعيل "كلمة مرور التطبيق" (App Password):**
    - اذهب لحساب Google -> الأمان (Security).
    - فعل "التحقق بخطوتين" (2-Step Verification).
    - ابحث عن "كلمات مرور التطبيقات" (App Passwords).
    - أنشئ واحدة جديدة باسم "AlSamer Website" وانسخ الكود المكون من 16 حرفاً.
3.  **إضافة المتغيرات في Vercel:**
    - اذهب لـ Vercel -> Settings -> Environment Variables.
    - أضف `EMAIL_USER` وضع فيه إيميل الـ Gmail الخاص بك.
    - أضف `EMAIL_PASS` وضع فيه كود الـ 16 حرفاً (بدون مسافات).

---

## 💻 كيف أغير الإيميلات أو الأرقام؟

اذهب إلى ملف: `src/config/contact.ts`

### 1️⃣ لتغيير الإيميلات التي يراها المستخدم:

قم بتعديل قائمة `recipients`.

- **label**: هو ما يراه المستخدم على الشاشة.
- **value**: هو الإيميل الذي سيتم الإرسال إليه (في العنوان).

### 2️⃣ لتغيير أرقام الهاتف أو الواتساب:

قم بتعديل قائمة `phones`.

- **label**: الرقم الذي يظهر للمستخدم.
- **value**: الرقم الدولي بدون (+) لكي يعمل رابط الواتساب تلقائياً.

---

## ⚠️ هام جداً: عندما تغير "الإيميل الرئيسي" (Primary Email)

الإيميل الرئيسي هو الذي يستلم كل الرسائل. إذا قمت بتغييره في المستقبل إلى إيميل حقيقي وجديد، يجب عليك اتباع هذه الخطوات:

1.  **في كود الموقع**:
    - افتح `src/config/contact.ts` وغير `primaryEmail` إلى الإيميل الجديد.
    - افتح `src/config/web3forms.ts` وضع مفتاح الـ `ACCESS_KEY` الجديد الخاص بهذا الإيميل.

2.  **في موقع Web3Forms**:
    - يجب أن تذهب لـ [Web3Forms](https://web3forms.com/) وتسجل بالإيميل الجديد لتحصل على مفتاح (Access Key) خاص به.

---

## 💡 لماذا هذا النظام هو الأفضل؟

- **سهولة التغيير**: تعدل في مكان واحد، فيتغير الموقع بالكامل (Contact Section, Footer, Modals).
- **الاستقرار**: لا يوجد خطر من حذف كود برمجي بالخطأ.
- **الاحترافية**: جميع الرسائل مشفرة ومحمية عبر نظام Web3Forms.

---

**ملاحظة**: الملف الآن يحتوي على العناوين الافتراضية، ويمكنك تغييرها في أي وقت بمجرد فتح الملف وتعديل النصوص داخل علامات التنصيص `" "`.
